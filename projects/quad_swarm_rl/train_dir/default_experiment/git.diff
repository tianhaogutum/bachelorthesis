diff --git a/gym_art/quadrotor_multi/quad_experience_replay.py b/gym_art/quadrotor_multi/quad_experience_replay.py
index a034b8d..17c4f1a 100644
--- a/gym_art/quadrotor_multi/quad_experience_replay.py
+++ b/gym_art/quadrotor_multi/quad_experience_replay.py
@@ -181,7 +181,7 @@ class ExperienceReplayWrapper(gym.Wrapper):
             obs = event.obs
             replayed_env = deepcopy(env)
             replayed_env.scenes = self.env.scenes
-            self.curr_obst_density = replayed_env.obst_density
+            self.curr_obst_density = replayed_env.obst_density if hasattr(replayed_env, "obst_density") else  self.curr_obst_density 
 
             # we want to use these for tensorboard, so reset them to zero to get accurate stats
             replayed_env.collisions_per_episode = replayed_env.collisions_after_settle = 0
